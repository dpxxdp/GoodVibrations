import math

def PitchSpaceToPitchClassSpace(psNotes):
	"""takes an array of psNotes (any integer) and converts them into pcsNotes (integers 0-12)
	pcsNotes = []
	for note in psNotes:
		pcs.append(note%12)
	return pcsNotes
	

def PitchClassSpaceToKey(pcsNotes):
	keyAsChar = 
	
		


























